services:
  web:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - db_data:/app/data
    environment:
      - NODE_ENV=production
      - DATABASE_URL=file:./data/diyabet.db
    restart: unless-stopped

volumes:
  db_data:
